# Le journal a une seule source, et les copies se refont toutes seules.
#
# ## Ce qui n'allait pas
#
# `SYNCHRONISATION.md` était tenu **à la main dans trois dépôts**, avec la
# consigne de porter chaque entrée chez les voisins. La consigne est écrite en
# tête du fichier, elle est claire, et elle a été appliquée quatre fois sur
# cinq pendant des mois.
#
# Une règle appliquée à 80 % ne produit pas un fichier juste à 80 %. Elle
# produit **trois fichiers dont aucun ne fait autorité** — c'est le verdict
# qu'affichait `scripts/concorder-la-synchronisation.py` le 11 septembre 2026 :
# trois empreintes, et la racine avec quinze jours de retard.
#
# ## Deux choses que cet en-tête disait mal, corrigées le 11 septembre 2026
#
# **« quinze jours de retard » est faux, et le mot « retard » l'est aussi.**
# Mesuré ce soir : la racine porte 30 entrées datées quand l'app en porte 80.
# Elle est sous la moitié du contenu. Un retard se rattrape en rejouant ce qui
# manque ; un écart de cet ordre se **remplace**.
#
# **Et « trois dépôts » n'est pas un inventaire des exemplaires.** Il y en a
# **seize** sous `~/ONTBible/` — dont **dix worktrees**, parce que le projet a
# fait du worktree sa règle quand plusieurs sessions travaillent. L'auteur
# l'avait prévu le 25 août : *il y en aura d'autant plus qu'on appliquera la
# règle*. On l'a appliquée.
#
# ## Ce que ce mécanisme ne fait donc pas, et il faut le savoir avant de s'en
# ## servir
#
# Il dispatche vers des **dépôts**. Trois exemplaires se refont ; les treize
# autres ne bougent pas.
#
# Douze d'entre eux sont des worktrees et se rattrapent par un `git pull` —
# c'est vrai, et ça ne se lit nulle part, donc c'est écrit ici.
#
# **Le seizième n'a aucun recours.** `~/ONTBible/SYNCHRONISATION.md` n'est dans
# aucun dépôt : aucun `pull` ne l'atteint, aucune fusion ne le corrige, et un
# workflow GitHub n'a pas de main sur ce chemin. C'est pourtant celui que
# `CLAUDE.md` charge dans toute session ouverte depuis la racine, c'est-à-dire
# celles qu'on demande d'ouvrir.
#
# Aujourd'hui il se rattrape à la main. **Le jour où ce mécanisme entrera en
# service, il fera trois copies fraîches et figera la quatrième** — non parce
# qu'il la casse, mais parce qu'il retire la dernière raison de la remarquer.
#
# Ce trou est connu, mesuré, et non traité. Le combler demande un bras qui
# touche le disque — un hook local, ou un contrôle qui rougit quand la racine
# diverge — et non un dispatch. C'est une décision de conception, pas un
# correctif, et elle appartient à l'auteur.
#
# ## Pourquoi un contrôle n'aurait pas suffi
#
# Le premier remède envisagé était de **détecter** la divergence. Une passe de
# réconciliation l'a disqualifié en une mesure : deux blocs étaient déjà dans un
# **ordre différent** entre l'app et le vault, sans qu'aucune entrée ne manque.
#
# Deux fichiers peuvent porter exactement le même texte et ne pas être
# identiques. Un contrôle aurait donc rougi sur l'ordre, puis sur une virgule,
# puis sur une reformulation — et chaque rougissement aurait demandé un
# arbitrage humain sur un fichier de notes.
#
# On ne détecte pas la divergence : **on supprime la duplication**.
#
# ## Ce que fait ce workflow
#
# Le vault porte le journal. Quand il change, ce workflow prévient les deux
# autres dépôts, qui vont chercher le fichier et ouvrent une PR chez eux.
#
# La copie qu'ils reçoivent porte un en-tête qui dit de ne pas l'éditer. C'est
# le même principe que `Schema.swift` et `Schema.kt`, engendrés depuis
# `schema.rs` : personne ne les édite, donc ils ne peuvent pas diverger.
#
# ## Les entrées locales ne voyagent pas
#
# Un dépôt a des choses à noter qui ne regardent que lui — l'app en a treize.
# Elles vivent dans son `SYNCHRONISATION-locale.md`, écrit à la main, que ce
# workflow ne touche jamais.
#
# ## Ce qu'il ne duplique pas, délibérément
#
# `propager.yml` recopie une liste de chemins depuis `pipeline/src/config.rs`,
# et son propre en-tête dit que cette duplication **a déjà échoué** : `lexique/`
# fut ajouté au pipeline le 20 août 2026, la liste ne l'a pas su, et les fiches
# corrigées ne partaient plus en ligne.
#
# Ici, un seul fait est écrit : le nom du fichier. Il n'est recopié nulle part
# ailleurs — les dépôts qui le reçoivent le lisent dans l'événement.

name: Journal

on:
  push:
    branches: [main]
    paths:
      - SYNCHRONISATION.md
  # Pour rejouer une propagation sans toucher au fichier — après un secret
  # renouvelé, ou pour rattraper un dépôt créé depuis.
  workflow_dispatch:

jobs:
  prevenir:
    runs-on: ubuntu-latest

    steps:
      # Le jeton s'éprouve avant de servir, et sur le droit exact dont on a
      # besoin. Trois pannes se ressemblent sans se dire : le secret absent, le
      # jeton expiré — un jeton fin dure un an au plus et s'éteint sans
      # prévenir —, et le jeton dont le propriétaire est le compte personnel
      # plutôt que l'organisation, qui n'obtient qu'une lecture.
      #
      # C'est la garde de `propager.yml`, et elle est reprise ici parce qu'elle
      # a déjà servi : sans elle, `gh` rend une erreur d'authentification qui ne
      # dit pas laquelle des trois.
      - name: Le jeton peut déclencher
        env:
          GH_TOKEN: ${{ secrets.DECLENCHEUR }}
        run: |
          set -euo pipefail
          if [ -z "${GH_TOKEN:-}" ]; then
            echo "::error::Le secret DECLENCHEUR manque. Voir l'en-tête de propager.yml."
            exit 1
          fi
          for depot in ONTBibleApp ONTBibleWebapp; do
            if ! gh api "repos/ONTBible/$depot/actions/permissions" >/dev/null 2>&1; then
              echo "::error::DECLENCHEUR n'a pas le droit Actions sur $depot."
              echo "::error::Vérifier : resource owner = ONTBible, permission Actions = Read and write, jeton non expiré."
              exit 1
            fi
          done
          echo "Le jeton ouvre les deux dépôts."

      # Le receveur existe-t-il chez chacun ?
      #
      # `gh workflow run` échoue tant que le fichier visé n'est pas sur `main`
      # du dépôt visé — et son message parle d'un workflow introuvable, ce qui
      # envoie chercher le défaut dans la configuration du jeton. C'est une
      # **quatrième panne qui ressemble aux trois précédentes**, et l'étape
      # ci-dessus l'aurait laissée passer en disant que tout va bien.
      #
      # Elle mord dans deux cas réels : si ce déclencheur fusionne avant les
      # receveurs, et pour tout dépôt ajouté plus tard — que le
      # `workflow_dispatch` de ce fichier prévoit explicitement, et qui n'aura
      # pas de receveur son premier jour.
      #
      # Relevé par la session du site, qui venait de tenir le même ordre sur le
      # contrat des nœuds : **le receveur d'abord, l'émetteur ensuite.**
      - name: Le receveur existe chez chacun
        env:
          GH_TOKEN: ${{ secrets.DECLENCHEUR }}
        run: |
          set -euo pipefail
          manquants=""
          for depot in ONTBibleApp ONTBibleWebapp; do
            if ! gh api "repos/ONTBible/$depot/actions/workflows/journal.yml" >/dev/null 2>&1; then
              manquants="$manquants $depot"
            fi
          done
          if [ -n "$manquants" ]; then
            echo "::error::Aucun receveur sur main de :$manquants"
            echo "::error::Le journal ne peut pas y être rapporté tant que journal.yml n'y est pas fusionné."
            echo "::error::Ce n'est pas un problème de jeton — l'étape précédente l'a éprouvé."
            exit 1
          fi
          echo "Les deux receveurs sont en place."

      - name: Prévenir les deux copies
        env:
          GH_TOKEN: ${{ secrets.DECLENCHEUR }}
        run: |
          set -euo pipefail
          for depot in ONTBibleApp ONTBibleWebapp; do
            # **La branche se demande, elle ne s'écrit pas.**
            #
            # `--ref main` était faux pour ONTBibleApp, qui n'a pas de `main` :
            # ses paliers ont été renommés le 31 août 2026 en `device`, `dev`,
            # `beta-test`, `app-store`. Le déclenchement aurait échoué chez lui
            # sur un message parlant d'une référence introuvable — sans dire
            # que la branche avait changé de nom.
            #
            # Et le contrôle qui l'a laissé passer mérite d'être nommé :
            # `gh api repos/…/branches/main` **réussit** sur un dépôt sans
            # `main`, en rendant la branche par défaut. Un test `&& echo oui`
            # répond donc « oui » parce que l'appel a abouti, pas parce que la
            # branche existe. C'est `git ls-remote --heads` qui tranche.
            defaut=$(gh repo view "ONTBible/$depot" --json defaultBranchRef -q .defaultBranchRef.name)
            if [ -z "$defaut" ]; then
              echo "::error::Branche par défaut illisible pour $depot."
              exit 1
            fi
            gh workflow run journal.yml --repo "ONTBible/$depot" --ref "$defaut"
            echo "Prévenu : $depot (sur $defaut)"
          done
